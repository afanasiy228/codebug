# Сколько простых

Дано число `n`. Найдите, сколько простых чисел находится в диапазоне `2..n`.

## Формат ввода
Одно число `n`.

## Формат вывода
Количество простых чисел на отрезке `2..n`.

Ограничения:
- 1 <= n <= 200000
