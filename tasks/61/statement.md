Дано дерево (корень 1). Для каждого запроса найдите LCA(u, v).

Формат ввода:
- n
- n-1 ребро
- q
- q пар u v

Формат вывода:
- LCA для каждой пары

Ограничения:
- Все входные данные помещаются в 64-битный знаковый тип.
