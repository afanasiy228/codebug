1) Это задача о минимальном гамильтоновом цикле для малого `n`.
2) Используйте динамику по битмаске: `dp[mask][v]` — минимальная стоимость
   прийти в `v`, посетив вершины из `mask`.
3) После заполнения таблицы добавьте стоимость возврата в точку `1`.
